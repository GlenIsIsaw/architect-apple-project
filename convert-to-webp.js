import e from"sharp";import r from"fs";import t from"path";let inputFolder=t.join(process.cwd(),"src/assets"),outputFolder=t.join(process.cwd(),"src/assets-webp");r.existsSync(outputFolder)||r.mkdirSync(outputFolder,{recursive:!0}),r.readdirSync(inputFolder).forEach(o=>{let i=t.extname(o).toLowerCase(),p=t.basename(o,i);if([".jpg",".jpeg",".png"].includes(i)){let n=t.join(inputFolder,o),s=t.join(outputFolder,p+".webp");if(r.existsSync(s)){console.log(`⏩ Skipped (already exists): ${p}.webp`);return}e(n).webp({quality:80}).toFile(s).then(()=>console.log(`✅ Converted: ${o} → ${p}.webp`)).catch(e=>console.error(`❌ Error converting ${o}:`,e))}});